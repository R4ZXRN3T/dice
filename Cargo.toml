[package]
name = "dice"
version = "1.0.0"
edition = "2024"

[dependencies]
rand = "0.10.0-rc.6"
clap = { version = "4.5.54", features = ["derive"] }

[profile.release]
strip = true
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"